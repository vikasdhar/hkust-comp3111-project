<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/color_red"
    android:orientation="vertical" >

    <ListView
        android:id="@android:id/list"
        android:layout_width="fill_parent"
        android:layout_height="0dp"
        android:layout_weight="0.9"
        android:divider="@null"
        android:dividerHeight="0dp" />

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="0dp"
        android:layout_weight="0.1"
        android:background="@drawable/color_white"
        android:orientation="horizontal" >

        <TextView
            android:id="@+id/sortByPlayListChild_playListIndicator"
            android:layout_width="0dp"
            android:layout_height="fill_parent"
            android:layout_marginLeft="10dp"
            android:layout_weight="0.85"
            android:ellipsize="middle"
            android:gravity="center_horizontal|left|fill"
            android:singleLine="true"
            android:text="PlayList Title"
            android:textColor="#666666"
            android:textSize="20sp" />

        <ImageButton
            android:id="@+id/sortByPlayListChild_playAll"
            style="?android:borderlessButtonStyle"
            android:layout_width="0dp"
            android:layout_height="fill_parent"
            android:layout_weight="0.15"
            android:src="@drawable/ic_action_play" />
    </LinearLayout>

</LinearLayout>